{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build-debug",
            "command": "msbuild", // or your build command
            "args": [
                "/property:Configuration=Debug",
                "${workspaceFolder}/build/gameoflife.sln"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}